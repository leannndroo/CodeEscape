<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.CodeEscape" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CodeEscape" parent="Base.Theme.CodeEscape" />

    <!-- Tema oscuro para diálogos del juego -->
    <style name="DialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:background">#1A1A1A</item>
        <item name="android:textColor">#00FF41</item>
        <item name="colorAccent">#00FF41</item>
    </style>
    <!-- Tema pantalla completa para el juego -->
    <style name="Theme.CodeEscape.FullScreen" parent="Theme.CodeEscape">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>